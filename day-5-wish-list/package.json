{
  "name": "day-5-wish-list",
  "version": "0.1.0",
  "description": "A wish list crm extension that uses custom objects to store wish list items per contact",
  "scripts": {
    "postinstall": "cd ./src/app/extensions/ && npm install && cd ../app.functions && npm install",
    "build": "npm run build --prefix ./src/app/extensions/"
  }
}
